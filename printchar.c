//declare a char variable and intialize it to 'A' then print a message along w the 
//char variable
#include <stdio.h>

int main(){
    char alpha;
    alpha = 'A';

    printf("This is the char alpha : %c\n", alpha);
    return 0;
}